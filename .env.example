# Copy to `.env` and fill in your backend URL.
# The form-data module posts to: ${VITE_API_URL}/create-form-data
VITE_API_URL=https://houseofonzone.com/crm
# "client" (default) or "family" — sent as `form_type` in every submission
VITE_FORM_TYPE=client
